---
id: scribe_release_audit_20260403-checklist
title: "\u2705 Acceptance Checklist \u2014 scribe_release_audit_20260403"
doc_type: CHECKLIST
doc_name: CHECKLIST
category: engineering
status: draft
version: '0.1'
last_updated: 2026-04-05 09:30:40 UTC
maintained_by: Corta Labs
created_by: Corta Labs
owners:
- ArchitectAgent
related_docs: []
tags: []
summary: ''
---
# ✅ Acceptance Checklist — scribe_release_audit_20260403

## Release Governance
<!-- ID: documentation_hygiene -->
- [x] <!-- id: arch-stage-2-synthesis --> Stage 2 architecture/planning synthesis is published in `ARCHITECTURE_GUIDE.md`, `PHASE_PLAN.md`, and `CHECKLIST.md` with the stable-release boundary made explicit. Proof: re-planned on 2026-04-04 from `RESEARCH_PHASE_4_1_STORAGE_CONFIG_CLEANUP_20260404.md` and `RESEARCH_TEST_SUITE_AUDIT.md`, updated again on 2026-04-04 after the failed Phase 5.1 signoff, narrowed once more on 2026-04-04 after the coherent `5.3` NO-SHIP rerun, amended again on 2026-04-04 after verified `phase54_probe` evidence showed the only remaining blocker was stale generated residue at `build/lib/scribe_mcp/config/vector_config.py`, then corrected again on 2026-04-04 after the post-implementation review FAIL with the bounded `5.6-A` / `5.6-B` / `5.6-C` addendum that preserves historical `5.3` / `5.5` evidence.
- [x] <!-- id: arch-release-boundary --> 1.0 scope explicitly excludes built-in vector support and public/shared-host runtime claims.
- [x] <!-- id: arch-plugin-contract --> Claude and Codex plugin distribution contracts are documented with manifest-only `.claude-plugin/` / `.codex-plugin/` directories, the exact public-safe allowlist (`scribe-architect`, `scribe-bug-hunter`, `scribe-coder`, `scribe-doc-writer`, `scribe-research-analyst`, `scribe-review-agent`, `scribe-security-agent`), the excluded-agent rule (`seshat`, `maat`, `ptah`, `sia`), the Claude hook-schema requirement, and non-destructive Codex projection/error-handling gates.

## Must Fix Before Stable Release

### Phase 1 — Security and surface hardening
<!-- phase: 1 -->
- [x] <!-- id: p1-transport-boundary --> SSE/REST require an application-layer auth boundary, REST backend routes share the same protection model as SSE/MCP, and default bind posture is safe/local-trusted. Proof: `src/scribe_mcp/server_sse.py` now rejects unauthenticated `/sse`, `/messages/`, and `/api/v1/*` requests behind one middleware boundary; `src/scribe_mcp/__main__.py` + `src/scribe_mcp/config/settings.py` default SSE bind to `127.0.0.1`; verified with `pytest -q tests/test_transport_sse.py tests/test_server_api.py tests/test_settings_schema_alias.py` (53 passed on 2026-04-03).
- [x] <!-- id: p1-trust-policy-surface --> Package 1.1 owns, publishes, and proves the single reusable transport/trust policy surface that Package 1.2 consumes for outside-repo allow/deny decisions. Proof: `src/scribe_mcp/server.py` publishes the runtime transport/trust surface through `build_transport_policy()` and `get_transport_policy()`, `src/scribe_mcp/config/settings.py` resolves the canonical outside-repo posture through `resolve_outside_repo_read_policy()`, `src/scribe_mcp/tools/read_file.py` consumes that surface via `_outside_repo_policy_state()`, `tests/test_transport_sse.py` proves the loopback-authenticated vs non-loopback runtime policy split, and `tests/test_phase1_package12_hardening.py` proves Package 1.2 consumes that same single surface for outside-repo allow/deny decisions.
- [x] <!-- id: p1-read-file-sandbox --> Outside-repo read defaults derive from transport/trust posture rather than per-repo env sprawl; the caller flag remains a request signal only; trusted local/stdio and loopback-only authenticated SSE permit `read_file(... allow_outside_repo=True)` by default; and Package 1.2 consumes the single canonical Package 1.1-owned trust-policy surface instead of recreating repo/env widening logic. Proof: `src/scribe_mcp/config/settings.py` derives the posture in `resolve_outside_repo_read_policy()`, `src/scribe_mcp/tools/read_file.py` treats `allow_outside_repo` as a request signal while deferring the allow/deny decision to `_outside_repo_policy_state()`, `tests/test_read_file_tool.py::test_read_file_allows_requested_outside_repo_reads_for_trusted_stdio` proves trusted stdio default allow, and `tests/test_phase1_package12_hardening.py` proves loopback-authenticated SSE default allow plus non-loopback/network default-deny unless explicitly force-enabled.
- [x] <!-- id: p1-read-file-override-precedence --> Global force-disable overrides every allow path, including deliberate non-loopback/network force-enable, and this precedence is proven in both trusted and network-exposed posture tests. Proof: `src/scribe_mcp/config/settings.py` sets `force_disable_outside_repo_reads` to override every allow path, `src/scribe_mcp/tools/read_file.py` returns `outside_repo_reads_disabled_by_global_policy` when force-disabled, `tests/test_read_file_tool.py::test_read_file_global_force_disable_beats_trusted_stdio_default_allow` proves disable beats the trusted stdio default-allow path, and `tests/test_phase1_package12_hardening.py::test_read_file_global_force_disable_beats_network_force_enable` proves disable also beats deliberate network force-enable.
- [x] <!-- id: p1-plugin-exec-policy --> Repo-local Python plugin execution is disabled by default or requires explicit trusted enablement with enforced integrity policy. Proof: `src/scribe_mcp/plugins/registry.py` now refuses repo-local plugins unless `plugin_config.enabled` is paired with trusted runtime opt-in via `SCRIBE_TRUST_REPO_PLUGINS` / `SCRIBE_ENABLE_EXTERNAL_PLUGINS`, and requires manifest + `file_hash` before import; `tests/test_phase1_package12_hardening.py::test_plugin_registry_requires_trusted_runtime_opt_in` and `::test_plugin_registry_loads_manifest_pinned_plugin_with_trusted_runtime` passed on 2026-04-03.
- [x] <!-- id: p1-search-safety --> The search tool no longer accepts unbounded attacker-controlled regex behavior on the public surface. Proof: `src/scribe_mcp/tools/search.py` now defaults to literal search, rejects multiline/advanced/backreference/nested-quantifier regex on the public surface, and `tests/test_phase1_package12_hardening.py::test_search_rejects_unsafe_nested_regex` passed on 2026-04-03.
- [x] <!-- id: p1-topology-docs --> Package 1.1-owned release docs explicitly describe loopback-authenticated SSE as a supported default-allow posture, explicitly describe non-loopback/network-exposed posture as default-deny unless force-enabled, document the global force-disable path for constrained environments, and reserve force-enable guidance for deliberate non-loopback/network exposure; Package 1.2 only consumes that contract. Proof: 2026-04-04 — `README.md:377-379` and `deploy/README.md:65-67` now publish the approved three-line posture split verbatim, and the wheel-only rebuild `python -m build --wheel --outdir /tmp/scribe_release_audit_20260403_dist .` regenerated clean `src/scribe_mcp.egg-info/PKG-INFO` plus wheel `METADATA` with no `vector`, `BertModel`, `torchvision`, or `multi-tenant` residue.

### Phase 2 — Slim core / remove built-in vector stack
<!-- phase: 2 -->
- [x] <!-- id: p2-base-deps-slim --> Base package metadata and install surfaces contain no `numpy`, `sentence-transformers`, `faiss`, torch-adjacent vector install path, or other built-in vector stack residue. Proof: 2026-04-04 release verification rebuilt `src/scribe_mcp.egg-info/{PKG-INFO,requires.txt}` and confirmed the dependency metadata still contains no vector deps; `requires.txt` now shows only `asyncpg~=0.29` for the `postgres` extra.
- [x] <!-- id: p2-runtime-vector-removed --> `src/scribe_mcp/plugins/registry.py` no longer imports/initializes a built-in `vector_indexer`, and core startup no longer loads vector runtime state. Proof: `PluginRegistry._load_builtin_plugins()` remains a no-op and the runtime/plugin smoke lane still passes via `pytest -q tests/test_phase1_package12_hardening.py` and `pytest -q tests/test_plugin_bundles.py` on 2026-04-04.
- [x] <!-- id: p2-tool-surface-removed --> Shipped vector MCP surfaces are removed from the core package: `src/scribe_mcp/tools/vector_search.py`, vector exports in `src/scribe_mcp/tools/__init__.py`, vector metadata/examples in `src/scribe_mcp/tools/base/tool_metadata.py`, and vector probe hooks in `src/scribe_mcp/scripts/scribe_probe.py`. Proof: 2026-04-04 Package `5.2-A` deleted the stale generated wheel-residue copies under `build/lib/scribe_mcp/{tools,plugins,scripts}/...`; the rebuilt wheel `/tmp/scribe_release_audit_20260403_52a_clean/scribe_mcp-2.2-py3-none-any.whl` now returns `0` matches for `vector_search|vector_indexer|check_vector_index|reindex_docs|reindex_vector`; and direct Scribe searches against `src/scribe_mcp/tools/__init__.py`, `src/scribe_mcp/tools/base/tool_metadata.py`, and `src/scribe_mcp/scripts/scribe_probe.py` each returned 0 matches for those removed file names.
- [x] <!-- id: p2-manage-docs-helper-cleanup --> `src/scribe_mcp/tools/manage_docs.py` no longer ships semantic/vector compatibility helpers such as `_chunk_text_for_vector` or `_resolve_semantic_limits`. Proof: the helper cleanup remains covered by `pytest tests/test_phase2_vector_core_removal.py -q` from the landed Phase 2 package, and no new release-verification evidence contradicted that narrower removal.
- [x] <!-- id: p2-semantic-contract --> Core `manage_docs(... action="search")` is text-only; `search_mode="semantic"` / `"vector"` explicitly fall back to text results with structured disclosure (`fallback_applied`, requested/effective mode, warning/reason), and repo config vector flags/semantic-limit helpers are removed from core. Proof: the Phase 2 regression contract remains the same, and the 2026-04-04 verification surfaced packaging residue rather than a runtime fallback regression.
- [x] <!-- id: p2-extension-seam --> Downstream semantic/vector add-ons target the retained `manage_docs` tool entrypoint plus `src/scribe_mcp/doc_management/actions/search.py`, backed by the plugin seam in `src/scribe_mcp/plugins/registry.py`, and do not depend on removed built-in vector helpers, aliases, or private imports. Proof: no 2026-04-04 release-verification failure contradicted the retained downstream extension seam; the blocker is shipped artifact residue, not the runtime plugin/search seam.
- [x] <!-- id: p2-vector-config-removed --> `settings.py`, repo config, `src/scribe_mcp/config/scribe_config_template.yaml`, and config templates no longer load or advertise built-in vector config/artifact generation, and the rebuilt release artifacts no longer ship `scribe_mcp/config/vector_config.py`. Proof: 2026-04-04 Package `5.4` completed the wheel-only cleanup by deleting stale generated `build/lib/scribe_mcp/config/vector_config.py`, after which `rg -n "vector_config" src/scribe_mcp pyproject.toml MANIFEST.in build/lib/scribe_mcp/config` returned no matches, `python -m build --wheel --outdir /tmp/scribe_release_audit_20260403_dist/phase54_probe .` rebuilt successfully, and `python -m zipfile -l /tmp/scribe_release_audit_20260403_dist/phase54_probe/scribe_mcp-2.2-py3-none-any.whl | grep -En 'vector_search|vector_indexer|check_vector_index|reindex_docs|reindex_vector|vector_config'` returned no matches.
- [x] <!-- id: p2-vector-blast-radius-cleaned --> The concrete vector blast-radius files are scrubbed from core release scope: `docs/Scribe_Usage.md`, `docs/whitepapers/scribe_mcp_whitepaper.md`, `src/scribe_mcp/config/scribe_config_template.yaml`, `src/scribe_mcp/scripts/reindex_vector.py`, `src/scribe_mcp/scripts/reindex_docs.py`, `src/scribe_mcp/scripts/check_vector_index.py`, and true vector tests, while non-vector helper coverage is preserved. Proof: 2026-04-04 Package `5.4` proved the final remaining blast-radius residue was only the stale generated wheel input at `build/lib/scribe_mcp/config/vector_config.py`; deleting that single file was sufficient, no broader generated subtree cleanup was required, the follow-up `rg -n "vector_config" src/scribe_mcp pyproject.toml MANIFEST.in build/lib/scribe_mcp/config` returned no matches, and the rebuilt `phase54_probe` wheel scan returned no vector or `vector_config` matches.

### Phase 3 — Packaging and plugin distribution
<!-- phase: 3 -->
- [x] <!-- id: p3-pyproject-truth --> `pyproject.toml` is the single public install truth and legacy install surfaces no longer contradict it. Proof: `pyproject.toml` now keeps `asyncpg` behind `.[postgres]`, `requirements.txt` delegates to `-e .[dev]`, `install.sh` installs named profiles from pyproject, and the wheel built with `python -m pip wheel --no-deps --no-build-isolation --wheel-dir /tmp/scribe_phase31_wheels .` on 2026-04-03 shows only base `Requires-Dist` entries plus `asyncpg~=0.29; extra == "postgres"`.
- [x] <!-- id: p3-install-profiles --> The release docs show clean install/setup profiles for SQLite default, Postgres extra, and trusted SSE deployment. Proof: `README.md` now publishes SQLite `python -m pip install .`, Postgres `python -m pip install ".[postgres]"`, and trusted SSE `scribe-server-sse` flows, while `install.sh [sqlite|postgres|trusted-sse|dev|dev-postgres]` mirrors those profiles from package metadata.
- [x] <!-- id: p3-portable-config-assets --> Shipped MCP/config examples use packaged entry points and contain no absolute developer-machine paths or repo-root-only shims. Proof: `src/scribe_mcp/config/mcp_config.json` now ships `"command": "scribe-server"` with only `SCRIBE_STORAGE_BACKEND=sqlite`, and searches on 2026-04-03 returned zero `SCRIBE_ROOT`, `REPO_ROOT`, `python -m server`, or `/home/` matches in `README.md` or the shipped config asset.
- [x] <!-- id: p3-claude-plugin --> The Claude bundle keeps only `.claude-plugin/plugin.json` inside `.claude-plugin/`, ships only allowlisted public-safe agents if any (`scribe-architect`, `scribe-bug-hunter`, `scribe-coder`, `scribe-doc-writer`, `scribe-research-analyst`, `scribe-review-agent`, `scribe-security-agent`), excludes `seshat`, `maat`, `ptah`, and `sia`, strips Council/private-agent logic from every public Claude asset, and uses a host-valid `plugins/claude/hooks/hooks.json` with a top-level `hooks` object. Proof: `plugins/claude/agents/` now contains only the seven allowlisted public-safe cards, `plugins/claude/agents/{maat,ptah,seshat,sia}.md` were removed, the shipped public Claude cards were rewritten on 2026-04-03 to remove the prior hidden-authority/private-workflow wording, and `plugins/claude/hooks/hooks.json` now wraps `PreToolUse` under a top-level `hooks` object with the managed-doc protection command.
- [x] <!-- id: p3-codex-plugin --> The Codex bundle keeps only `.codex-plugin/plugin.json` inside `.codex-plugin/`; `plugins/codex/assets/agents.json`, any other Codex projection catalogs, and all projected `agents/*.toml` / projected markdown outputs enumerate only the allowlisted public-safe slugs, never `seshat`, `maat`, `ptah`, or `sia`, and contain no Council/private-agent logic or Council-only workflow/orchestration text; `.agents/plugins/marketplace.json` points `source.path` at the Codex plugin root; projection preserves existing user config/files by default and is idempotent; and the public CLI handles projection/config/filesystem failures with clean user-facing errors. Proof: `plugins/codex/agents/`, `plugins/codex/assets/agents/`, and `plugins/codex/assets/agents.json` now contain only the seven allowlisted slugs; `.agents/plugins/marketplace.json` still points at `./plugins/codex`; and `src/scribe_mcp/scripts/project_codex_plugin.py` now normalizes and rejects private/internal markers in catalog descriptions plus shipped projection templates/instruction assets before projection while still preserving existing user agent files, merging config non-destructively, and reporting unchanged vs preserved-existing outcomes.
- [x] <!-- id: p3-plugin-smoke-tests --> Plugin smoke tests prove the official Claude/Codex layouts, the Claude hook schema, the public-safe allowlist and excluded-agent rule, the absence of Council/private logic in public bundle assets, Codex projection catalogs, and projected outputs, non-destructive Codex merge/idempotence behavior, and clean user-facing CLI handling for malformed config, projection/config failures, and filesystem error cases. Proof: `pytest tests/test_plugin_bundles.py -v` passed 8/8 on 2026-04-03, covering the Claude/Codex root layouts, the top-level Claude hook schema, allowlist/exclusion enforcement, no-private-logic scans across shipped catalog descriptions plus shipped/projected markdown and projected TOML outputs, non-destructive/idempotent Codex projection behavior, and clean CLI failures for missing-manifest, malformed-config, filesystem, and invalid-catalog validation cases.

### Phase 4 — Storage, config, and release-surface cleanup
<!-- phase: 4 -->
- [x] <!-- id: p4-postgres-vector-residue --> Package `4.1-A` removes vector-extension runtime/bootstrap residue from `storage/postgres/schema.py` and `bootstrap_postgres.py` without disturbing legitimate non-vector bootstrap behavior. Proof: 2026-04-04 — `src/scribe_mcp/storage/postgres/schema.py` and `src/scribe_mcp/scripts/bootstrap_postgres.py` no longer execute `CREATE EXTENSION IF NOT EXISTS vector;`, the bootstrap intro now says `Enable pg_trgm`, Scribe search for `vector|pgvector` across those two files returned 0 matches, and `pytest -q tests/test_bootstrap_postgres_script.py` passed 14/14 with focused bootstrap/runtime coverage.
- [x] <!-- id: p4-public-storage-contract --> Package `4.1-B` publishes SQLite, Postgres, and remote/client mode as the supported public 1.0 storage contract, clearly separating runtime envs from bootstrap-only envs. Proof: 2026-04-04 — `README.md`, `docs/Scribe_Usage.md`, and `docs/GLOBAL_DEPLOYMENT_GUIDE.md` now publish SQLite, Postgres, and remote/client as the three public 1.0 storage modes while separating runtime envs from bootstrap-only Postgres bootstrap conveniences, `src/scribe_mcp/config/settings.py` exports `PUBLIC_STORAGE_MODES` / `PUBLIC_STORAGE_SETTINGS_CONTRACT`, and `pytest -q tests/test_settings_schema_alias.py tests/test_settings_public_contract.py` passed 6/6.
- [x] <!-- id: p4-public-settings-clarity --> Package `4.1-B` / `4.1-D` keep settings public and consumer-tweakable by documenting canonical settings, compatibility aliases, and advanced public settings rather than hiding or silently deleting them. Proof: 2026-04-04 — `src/scribe_mcp/config/settings.py` now labels the public storage env surface as canonical, compatibility, advanced/public, or bootstrap-only, and focused tests prove `SCRIBE_DB_SCHEMA` / `SCRIBE_SQLITE_PATH` remain visible compatibility aliases rather than being hidden or removed.
- [x] <!-- id: p4-portable-path-policy --> Phase 4 docs/examples/artifact paths use portable config/env/public-setting defaults and do not commit any operator-specific direct path. Proof: 2026-04-04 — the refreshed public docs/examples use portable values such as `./data/scribe_projects.db`, `/path/to/...`, and `https://scribe.example.internal`; targeted Scribe searches for `/home/austin|/Users/|C:\\Users\\` across `README.md`, `docs/Scribe_Usage.md`, and `docs/GLOBAL_DEPLOYMENT_GUIDE.md` returned 0 matches.
- [x] <!-- id: p4-mode-resolution --> Package `4.1-C` makes SQLite, Postgres, and remote/client startup deterministic against one documented mode-selection contract. Proof: 2026-04-04 — `src/scribe_mcp/config/mode_detection.py` now centralizes sync configured-mode resolution via `resolve_configured_mode()`, `src/scribe_mcp/storage/__init__.py` derives import-time backend selection from that shared three-mode contract, `src/scribe_mcp/server.py` now rebinds the live backend from the resolved startup mode for client/server/standalone outcomes, and `pytest -q tests/test_mode_detection.py tests/test_runtime_mode_resolution.py` passed 13/13 with focused proof for deterministic remote selection and startup singleton rebinding.
- [x] <!-- id: p4-storage-model-compat --> Package `4.1-D` removes or clearly deprecates vector-only storage-model residue without breaking a documented public import surface. Proof: 2026-04-04 — direct Scribe searches for `VectorIndexRecord` and `VectorShardMetadata` found no live `src/`, `tests/`, or `README.md` import/documentation surface outside `src/scribe_mcp/storage/models.py`, so `src/scribe_mcp/storage/models.py` removed both dead vector dataclasses instead of shipping compatibility shims, `from scribe_mcp.storage.models import Vector...` now matches only `.scribe/backups/*`, `src/scribe_mcp/storage/models.py` contains 0 `Vector*` symbols, and `pytest -q tests/test_storage_models_compatibility.py` passed 1/1 proving the module still imports while omitting the removed vector residue.
- [x] <!-- id: p4-collect-clean --> Package `4.2-A` restores clean collection for `pytest --collect-only tests -q`. Proof: 2026-04-04 — `tests/core/test_manage_docs_semantic_limits.py` (moved under the new shipped-core lane during `4.2-C`) no longer imports the removed `_resolve_semantic_limits` helper, `pytest -q tests/core -m "not slow and not performance"` passed 3/3 on that core shard, and the earlier package gate `pytest --collect-only tests -q` completed cleanly with `2152/2155 tests collected (3 deselected)` and no collection errors.
- [x] <!-- id: p4-builtins-mutation --> Package `4.2-A` removes the `manage_docs_validation` import-time `builtins` mutation. Proof: 2026-04-04 — `src/scribe_mcp/tools/manage_docs_validation.py` no longer registers helper symbols into `builtins`, and the new focused regression re-imports `scribe_mcp.tools.manage_docs_validation` to prove its helper API stays on the module while `builtins` never gains `ParameterValidationError`, `_validate_inputs`, `_validate_comparison_symbols`, or `create_manage_docs_validator`.
- [x] <!-- id: p4-test-namespace-clean --> Package `4.2-B` removes benchmark/demo/generated-artifact files from `tests/` and moves future artifact output to a gitignored non-test path. Proof: 2026-04-04 — `tests/benchmark_connection_pool.py` moved to `benchmarks/benchmark_connection_pool.py`, `tests/demo_spec_token_003.py` moved to `examples/demo_spec_token_003.py`, the eight committed `tests/performance_results_*.json` files were removed, `tests/test_performance.py` now defaults generated JSON output to `benchmarks/artifacts/` with `SCRIBE_PERFORMANCE_OUTPUT_DIR` as an env override, `.gitignore` now ignores `benchmarks/artifacts/`, `pytest -q tests/test_performance.py -m performance -k 'results_output_dir_defaults_to_gitignored_benchmarks_artifacts or save_results_honors_env_output_dir_override'` passed 2/2 (1 deselected), `find tests -maxdepth 1 -type f \( -name 'performance_results_*.json' -o -name 'benchmark_connection_pool.py' -o -name 'demo_spec_token_003.py' \) | sort` returned no files, and targeted searches across the touched files found 0 `/home/austin`, `/Users/`, or `C:\\Users\\` paths.
- [x] <!-- id: p4-test-contract --> Package `4.2-C` establishes `tests/core/`, `tests/integration/`, `tests/integration/storage/`, and the `core` / `integration` / `postgres` / `performance` / `slow` / `manual` marker contract with an explicit default fast lane. Proof: 2026-04-04 — `pytest.ini` now registers all six release markers and enforces them with `--strict-markers`, `tests/conftest.py` applies the path-based `core`/`integration` contract for the new directories, `tests/README.md` and `tests/data/README.md` document the shipped layout plus the deferred strict-`testpaths` note, `pytest -q tests/core -m "not slow and not performance"` passed 3/3, `pytest --collect-only -q tests/integration -m "not performance"` collected 48 integration tests, `pytest --collect-only -q tests/integration/storage -m postgres` collected 5/11 Postgres-only parametrizations with 6 deselections, and `pytest --collect-only -q tests/test_performance.py -m performance` collected 3 dedicated performance tests outside the default lane.
- [x] <!-- id: p4-test-naming-fixtures --> Package `4.2-D` converts the release-facing integration files to behavior-based names and consolidates recurring fixtures into area-local `conftest.py` files without regrowing the global harness. Proof: 2026-04-04 — `tests/integration/test_integration_phase5.py` became `tests/integration/test_toolkit_workflows.py`, `tests/integration/storage/test_storage_backend_conformance.py` became `tests/integration/storage/test_storage_backend_shared_contract.py`, the repeated `project_tree` / `router` and backend lifecycle fixtures moved into `tests/integration/conftest.py` and `tests/integration/storage/conftest.py`, `pytest -q tests/integration/test_toolkit_workflows.py::TestSessionTracking::test_record_and_check_file_read` passed 1/1, `pytest -q tests/integration/storage/test_storage_backend_shared_contract.py -m "not postgres" -k project_entry_roundtrip` passed 1/1 with 10 deselections, `pytest -q tests/core -m "not slow and not performance"` still passed 3/3, `pytest --collect-only -q tests/integration -m "not performance"` still collected 48 tests, and `pytest --collect-only -q tests/integration/storage -m postgres` still collected 5/11 tests with 6 deselections.

### Phase 5 — Release remediation and signoff
<!-- phase: 5 -->
- [x] <!-- id: p5-no-ship-evidence-captured --> The failed 2026-04-04 signoff and the coherent `5.3` rerun are preserved as historical evidence and not rewritten as if the candidate passed. Proof: `PHASE_PLAN.md` now keeps Package `5.1` as the locked no-ship gate, Packages `5.2-A` / `5.2-B` / `5.2-C` as historical remediation proofs, and Package `5.3` as the coherent NO-SHIP rerun that justified the narrow `5.4` / `5.5` re-plan.
- [x] <!-- id: p5-wheel-surface-cleanup --> Package `5.2-A` removes the remaining vector-only wheel/package residue (`vector_search.py`, `vector_indexer.py`, `vector_indexer.json`, `check_vector_index.py`, `reindex_docs.py`, `reindex_vector.py`) and any helper export/metadata/probe references required to ship them. This package closes `p2-tool-surface-removed` and the wheel portion of the earlier tool/plugin blast radius. Proof: 2026-04-04 Package `5.2-A` deleted the stale generated residue under `build/lib/scribe_mcp/{tools,plugins,scripts}/...`, rebuilt `/tmp/scribe_release_audit_20260403_52a_clean/scribe_mcp-2.2-py3-none-any.whl`, confirmed `python -m zipfile -l ... | grep -Eic 'vector_search|vector_indexer|check_vector_index|reindex_docs|reindex_vector'` returned `0`, and confirmed direct Scribe searches on `src/scribe_mcp/tools/__init__.py`, `src/scribe_mcp/tools/base/tool_metadata.py`, and `src/scribe_mcp/scripts/scribe_probe.py` each returned 0 matches for those removed file names.
- [x] <!-- id: p5-sdist-sources-cleanup --> Package `5.2-B` removes `src/scribe_mcp/config/vector_config.py`, the Phase 2 vector-removal regression test, and any other vector-only residue from rebuilt sdists and regenerated `src/scribe_mcp.egg-info/SOURCES.txt` by fixing the real packaging inputs rather than hand-editing generated metadata. This package closes the sdist/manifest portion of the earlier release-surface blocker. Proof: 2026-04-04 — added root `MANIFEST.in` excludes for `src/scribe_mcp/config/vector_config.py` and `tests/test_phase2_vector_core_removal.py`, rebuilt with `python -m build --sdist --outdir /tmp/scribe_release_audit_20260403_52b .`, and confirmed both `grep -Eic 'vector|test_phase2_vector_core_removal' src/scribe_mcp.egg-info/SOURCES.txt` and `tar -tzf /tmp/scribe_release_audit_20260403_52b/scribe_mcp-2.2.tar.gz | grep -Eic 'vector|test_phase2_vector_core_removal'` returned `0`.
- [x] <!-- id: p5-release-doc-metadata-truth --> Package `5.2-C` cleans `README.md` / `deploy/README.md` so rebuilt `PKG-INFO` / wheel `METADATA` contain no vector, torch-warning, or multi-tenant residue and both docs publish the approved loopback-authenticated default-allow + global-force-disable + non-loopback default-deny-unless-force-enabled posture wording. Proof: 2026-04-04 — removed the stale Bert/torchvision warning block plus deferred `Multi-tenant` release copy from `README.md`, updated `README.md:377-379` and `deploy/README.md:65-67` to the approved wording verbatim, rebuilt with `python -m build --wheel --outdir /tmp/scribe_release_audit_20260403_dist .`, and confirmed `PKG-INFO_RESIDUE=NONE` / `WHEEL_METADATA_RESIDUE=NONE` from the follow-up scans against `src/scribe_mcp.egg-info/PKG-INFO` and the rebuilt wheel `METADATA`.
- [x] <!-- id: p5-wheel-vector-config-cleanup --> Package `5.4` owns the final wheel-only blocker as a generated-tree cleanup task: delete stale `build/lib/scribe_mcp/config/vector_config.py` before rebuilding, and clean the minimal `build/lib/scribe_mcp/config/` or `build/lib/scribe_mcp/` generated subtree only if that single-file removal is insufficient. Proof: 2026-04-04 source truth stayed fixed — `src/scribe_mcp/config/vector_config.py` remained deleted and `MANIFEST.in` remained free of the stale exclude — while the generated-tree cleanup deleted `build/lib/scribe_mcp/config/vector_config.py`, required no broader subtree removal, `rg -n "vector_config" src/scribe_mcp pyproject.toml MANIFEST.in build/lib/scribe_mcp/config` returned no matches, `python -m build --wheel --outdir /tmp/scribe_release_audit_20260403_dist/phase54_probe .` rebuilt successfully, and `python -m zipfile -l /tmp/scribe_release_audit_20260403_dist/phase54_probe/scribe_mcp-2.2-py3-none-any.whl | grep -En 'vector_search|vector_indexer|check_vector_index|reindex_docs|reindex_vector|vector_config'` returned no matches.
- [x] <!-- id: p5-final-release-rerun --> Package `5.5` rebuilt the artifacts once, reran the retained smoke lanes, rechecked the already-clean sdist / `SOURCES.txt` / `PKG-INFO` / wheel `METADATA` surfaces, and updated the governed docs from one final evidence bundle. Proof: 2026-04-04 final bundle = `/tmp/scribe_release_audit_20260403_dist/final_55/`; `python -m zipfile -l /tmp/scribe_release_audit_20260403_dist/final_55/scribe_mcp-2.2-py3-none-any.whl | grep -En 'vector_search|vector_indexer|check_vector_index|reindex_docs|reindex_vector|vector_config'`, `tar -tzf /tmp/scribe_release_audit_20260403_dist/final_55/scribe_mcp-2.2.tar.gz | grep -En 'vector_search|vector_indexer|check_vector_index|reindex_docs|reindex_vector|vector_config|test_phase2_vector_core_removal'`, `grep -En 'vector_search|vector_indexer|check_vector_index|reindex_docs|reindex_vector|vector_config|test_phase2_vector_core_removal' src/scribe_mcp.egg-info/SOURCES.txt`, `grep -Eni 'vector|BertModel|torchvision|Multi-tenant|multi-tenant' src/scribe_mcp.egg-info/PKG-INFO`, and `unzip -p /tmp/scribe_release_audit_20260403_dist/final_55/scribe_mcp-2.2-py3-none-any.whl '*/METADATA' | grep -Eni 'vector|BertModel|torchvision|Multi-tenant|multi-tenant'` all returned no matches; `README.md:377-379` and `deploy/README.md:65-67` still matched the approved posture wording; `pytest -q tests/test_transport_sse.py` (32 passed), `pytest -q tests/test_phase1_package12_hardening.py` (7 passed), `pytest -q tests/test_plugin_bundles.py` (8 passed), `pytest -q tests/core -m "not slow and not performance"` (3 passed), and the clean-target wheel-install smoke into `/tmp/scribe_release_audit_20260403_dist/final_55_install_smoke_20260404T2226Z` imported `scribe_mcp` successfully while `importlib.metadata.version('scribe-mcp')` resolved to `2.2`.
- [x] <!-- id: p5-ship-decision --> The current coherent rerun decision is recorded from `5.3` and remains NO-SHIP until a later rerun supersedes it. Proof: 2026-04-04 current decision = **NO-SHIP** from `/tmp/scribe_release_audit_20260403_dist/final_53` because the rebuilt wheel still contained `43:scribe_mcp/config/vector_config.py` while the sdist/`SOURCES.txt`/`PKG-INFO`/wheel `METADATA` scans and all retained smoke lanes stayed clean.
- [x] <!-- id: p5-post-54-ship-decision --> The post-`5.4` / `5.5` ship-or-no-ship decision is now recorded from the single coherent `final_55` rerun bundle without revising the historical `5.3` NO-SHIP record. Proof: 2026-04-04 final decision = **SHIP** because `/tmp/scribe_release_audit_20260403_dist/final_55/` proved the rebuilt wheel, sdist, regenerated `SOURCES.txt`, regenerated `PKG-INFO`, and wheel `METADATA` were all clean in the same rerun window while the retained smoke lanes and local wheel-install smoke also passed.
<!-- ID: final_verification -->
### Phase 5-B — Post-review correction closure
<!-- phase: 5b -->
- [x] <!-- id: p5-governed-truth-reconciled --> The governed docs are now coherent with the final release state: `p1-trust-policy-surface`, `p1-read-file-sandbox`, and `p1-read-file-override-precedence` are all closed with citations to the existing source/test chain; `5.6-A` through `5.6-E` are complete; historical `5.3` / `5.5` / `5.6-C` evidence is preserved; the prior NO-SHIP block is explicitly resolved as stale artifact drift after `5.6-B`; and current decision = **SHIP** from fresh bundle `/tmp/scribe_release_audit_20260403_dist/final_56d_20260404T231620Z`.
- [x] <!-- id: p5-runtime-vector-reminder-cleanup --> `src/scribe_mcp/doc_management/actions/edit.py` no longer emits `vector_index_docs=false`, `.scribe/config/scribe.yaml`, or `scripts/reindex_vector.py --docs` after successful edits; any retained reminder remains consistent with the text-only 1.0 `manage_docs` contract. Proof: 2026-04-04 — source reread of `src/scribe_mcp/doc_management/actions/edit.py` showed the successful-edit response now stops after `hashes` / `extra` / `index_warning` handling and no longer appends any `vector_index_docs`, `.scribe/config/scribe.yaml`, or `scripts/reindex_vector.py --docs` reminder text; `pytest -q tests/test_manage_docs_reminders.py` passed (3 passed), including `test_manage_docs_successful_edit_has_no_vector_runtime_reminder` to keep those removed strings and the `vector_index_docs` reminder category out of successful edit responses.
- [x] <!-- id: p5-post-review-signoff --> `5.6-C` completed the blocker-identification pass after `p5-runtime-vector-reminder-cleanup`: it re-read the governed Phase 1 trust-policy closures against the live source/runtime state, reran only `pytest -q tests/test_manage_docs_reminders.py -k successful_edit_has_no_vector_runtime_reminder` (1 passed, 2 deselected), confirmed `src/scribe_mcp/doc_management/actions/edit.py` had 0 matches for `vector_index_docs|reindex_vector|scribe.yaml`, and then inspected the historical `final_55` wheel surface at `/tmp/scribe_release_audit_20260403_dist/final_55/scribe_mcp-2.2-py3-none-any.whl`, where packaged `scribe_mcp/doc_management/actions/edit.py` still contained the removed reminder strings and `vector_index_docs` category at lines 256-260. This item captures the current NO-SHIP blocker as artifact drift after `5.6-B`, not as a live-source contradiction.
- [x] <!-- id: p5-post-56b-artifact-rerun --> `5.6-D` built the fresh post-`5.6-B` artifact/signoff bundle at `/tmp/scribe_release_audit_20260403_dist/final_56d_20260404T231620Z`, reran only `pytest -q tests/test_manage_docs_reminders.py -k successful_edit_has_no_vector_runtime_reminder` (`1 passed, 2 deselected`), confirmed live `src/scribe_mcp/doc_management/actions/edit.py` had 0 matches for `vector_index_docs|reindex_vector|scribe.yaml`, and confirmed rebuilt-wheel packaged `scribe_mcp/doc_management/actions/edit.py` also had 0 matches for those strings.
- [x] <!-- id: p5-post-56b-final-signoff --> `PHASE_PLAN.md` and `CHECKLIST.md` now record the final decision = **SHIP** from fresh bundle `/tmp/scribe_release_audit_20260403_dist/final_56d_20260404T231620Z` while preserving the historical `5.3`, `5.5`, and `5.6-C` proof text unchanged. The prior NO-SHIP block is explicitly resolved as stale artifact drift after `5.6-B`, not as any remaining live-source/runtime contradiction.

### Phase 6 — Synchronized public/core split + remote-auth compatibility wave
<!-- phase: 6 -->
- [x] <!-- id: p6-architecture-wave-published --> The next release-wave architecture is published in governed docs with the Phase 6 boundary decisions frozen and coder-ready: the optional council extension is locked to the same-repo `packages/scribe_council/` layout, the bridge surface is classified explicitly into core runtime vs extension-only vs local-only ownership, the only temporary compatibility shim is the one-wave deprecated `src/scribe_mcp/council_templates/__init__.py` import shim, remote/client remains optional-not-default, README/file-map work remains last, and `PHASE_PLAN.md` now contains the explicit shared-file ownership/merge-order map for `pyproject.toml`, `.gitignore`, runtime path/config surfaces, staged release docs, final `README.md`, final `docs/RELEASE_FILE_MAP.md`, and the shared package-boundary files touched across `6.1` / `6.2` / `6.3` / `6.4`. Proof: 2026-04-05 — `ARCHITECTURE_GUIDE.md` sections 9.1-9.4 now freeze the exact package layout, bridge classification, shim duration, and Phase 6 merge-order contract; `PHASE_PLAN.md` Phase Overview plus Packages `6.1-A` and `6.3-A` now assign first-write/finalization ownership for the collision-prone surfaces; this checklist item records that the architecture is now implementation-ready on those two previously blocked dimensions.
- [x] <!-- id: p6-core-extension-boundary --> Built `scribe-mcp` artifacts no longer ship the `council.templates` entry point or `council_templates/**/*`, and the optional extension package (`scribe-council` or final equivalent) owns that surface instead. Proof: 2026-04-05 correction — fresh clean builds from `/tmp/scribe_release_audit_20260403_6_1_a_fix/stage` produced `scribe_mcp-2.2-py3-none-any.whl` whose only `council_templates/` path is the deprecated shim module `scribe_mcp/council_templates/__init__.py`; no `__pycache__`, rules, or skills payloads remain in the core wheel, and no `[council.templates]` entry point is published there. The paired `scribe_council-2.2-py3-none-any.whl` still publishes `scribe-council = scribe_council.council_templates:get_council_templates` and ships the template payload; installing both wheels into `/tmp/scribe_release_audit_20260403_6_1_a_fix/install_check` confirmed the provider manifest resolves from `scribe_council` and the legacy shim emits its explicit next-breaking-release / no-later-than-v3 deprecation warning while forwarding successfully.
- [x] <!-- id: p6-runtime-path-hygiene --> `.scribe/cli/*.json` is fully treated as gitignored local runtime state, repo-root `state/*.json` writers are removed in favor of `.scribe/state/`, `.scribe/logs/` remains runtime-only, and code/docs/gitignore all agree on that boundary. Proof: 2026-04-05 — `src/scribe_mcp/config/paths.py` now centralizes the shared `.scribe/{cli,state,logs}` runtime namespace helpers/constants, `src/scribe_mcp/utils/rotation_state.py` and `src/scribe_mcp/utils/audit.py` now default to `.scribe/state/` via `runtime_state_dir()`, `src/scribe_mcp/object_store/keys.py` now denies `.scribe/state/` alongside `.scribe/cli/` and `.scribe/logs/`, and `docs/RELEASE_SURFACE.md` now treats those three namespaces as local runtime-only surfaces. Verification: `python -m compileall -q src/scribe_mcp/config/paths.py src/scribe_mcp/cli/session_store.py src/scribe_mcp/utils/rotation_state.py src/scribe_mcp/utils/audit.py src/scribe_mcp/object_store/keys.py tests/test_runtime_path_policy.py`; `pytest -q tests/test_runtime_path_policy.py` (`4 passed in 0.07s`); `git check-ignore -v .scribe/cli/runtime-policy.json .scribe/state/rotation_state.json`; `rg -n 'repo_root\(\) / "state"|state/\*\.json' src/scribe_mcp/config/paths.py src/scribe_mcp/cli/session_store.py src/scribe_mcp/utils/rotation_state.py src/scribe_mcp/utils/audit.py src/scribe_mcp/object_store/keys.py docs/RELEASE_SURFACE.md .gitignore`; `rg -n '\.scribe/(cli|state|logs)' src/scribe_mcp/config/paths.py src/scribe_mcp/object_store/keys.py docs/RELEASE_SURFACE.md .gitignore`.
- [x] <!-- id: p6-local-overlay-demotion --> Repo-local `.council/`, `.mcp.json`, `opencode.json`, and operator-specific `AGENTS.md` / `.claude/` / `.codex/` material are explicitly treated as local overlays rather than public release truth, and tracked docs/examples replace them in shipped guidance. Proof: 2026-04-05 — added `docs/RELEASE_SURFACE.md`, `docs/examples/mcp.json.example`, and `docs/examples/opencode.json.example`, and those tracked files now define the public bootstrap guidance instead of the repo-root overlay files. `.gitignore` now ignores `opencode.json`, `.scribe/cli/*.json`, `.scribe/state/`, `build/`, and `dist`; `MANIFEST.in` prunes `.council`, `.claude`, `.codex`, `.scribe`, `state`, `logs`, `build`, and `dist` while excluding `.mcp.json`, `opencode.json`, `AGENTS.md`, `CLAUDE.md`, and `tool_calls.jsonl`; narrow verification via `git check-ignore -v --stdin`, `rg -n`, and `python -m json.tool` passed.
- [x] <!-- id: p6-remote-client-auth --> Optional supported public `remote/client` mode loads a client auth token, `RemoteStorageBackend` sends it on remote requests, dedicated regressions now freeze that contract, and public docs now state clearly that local/standard core usage remains the default posture while remote/client stays optional and authenticated. Proof: 2026-04-05 — `src/scribe_mcp/config/settings.py` now publishes canonical `SCRIBE_REMOTE_AUTH_TOKEN` with fallback aliases to `SCRIBE_TRANSPORT_AUTH_TOKEN` / `SCRIBE_AUTH_TOKEN`; `src/scribe_mcp/storage/__init__.py` threads the resolved token into `RemoteStorageBackend`; `src/scribe_mcp/storage/remote.py` now attaches `Authorization: Bearer <token>` plus compatibility `x-scribe-auth` headers on backend and batch requests and raises explicit auth failures for `401`/`403`; `src/scribe_mcp/config/mode_detection.py` now treats auth rejection as a hard configuration error while preserving `SCRIBE_REMOTE_FALLBACK` for truly unavailable remotes. `tests/test_runtime_mode_resolution.py` now locks canonical-vs-alias client token loading plus backend token threading, `tests/test_remote_backend.py` now asserts auth headers on backend and batch requests plus explicit `401`/`403` guidance, and `docs/REMOTE_CLIENT.md` now states that local/standard core usage remains the shipped default, remote/client is optional and authenticated, the three release postures are loopback-local, managed private-mesh/Tailscale, and unsupported casual public exposure, and `0.0.0.0` guidance is allowed only for managed/private-mesh deployments with auth. Verification: `python -m compileall -q src/scribe_mcp/config/settings.py src/scribe_mcp/storage/__init__.py src/scribe_mcp/storage/remote.py src/scribe_mcp/config/mode_detection.py`; `pytest -q tests/test_remote_backend.py tests/test_runtime_mode_resolution.py` (`54 passed in 0.26s`); `rg -n "local/standard core usage|optional authenticated capability|Loopback-local|Managed private-mesh / Tailscale|Casual public exposure|0\.0\.0\.0|unsupported" docs/REMOTE_CLIENT.md`.
- [x] <!-- id: p6-bridge-extension-unification --> The shipped bridge surface now has one coherent ownership story: manifest discovery, plugin loading, policy enforcement, hook/runtime activation, and extension packaging all agree on what is core, optional extension, or local-only. Proof: 2026-04-05 — `src/scribe_mcp/bridges/runtime.py` now centralizes bridge runtime binding so registration requires a real runtime path (`plugin_factory` or explicit plugin class) and binds `BridgeToScribeAPI` + `BridgePolicyPlugin` into the plugin before hook registration; `src/scribe_mcp/bridges/registry.py` now rejects manifest-only runtime gaps, registers plugins with the configured hook manager, and records the runtime owner package; `src/scribe_mcp/bridges/api.py` now uses the async ownership-aware policy checks; `src/scribe_mcp/server.py` no longer tolerates manifest-only inactive bridge startup; and `docs/RELEASE_SURFACE.md` plus `docs/BRIDGE_DEVELOPMENT.md` now explicitly classify core bridge runtime vs optional extension assets vs local-only overlays while documenting the no-placeholder activation rule and `plugin_factory` manifest contract. Verification: `python -m compileall -q src/scribe_mcp/bridges/manifest.py src/scribe_mcp/bridges/plugin.py src/scribe_mcp/bridges/policy.py src/scribe_mcp/bridges/api.py src/scribe_mcp/bridges/runtime.py src/scribe_mcp/bridges/registry.py src/scribe_mcp/bridges/examples/hello_world_plugin.py src/scribe_mcp/server.py tests/test_bridge_registry.py tests/test_bridge_system.py`; `pytest -q tests/test_bridge_registry.py tests/test_bridge_system.py` (`85 passed in 25.95s`); `rg -n 'optional extension|local-only overlays|Manifest-only inactive placeholders|plugin_factory|runtime plugin' docs/RELEASE_SURFACE.md docs/BRIDGE_DEVELOPMENT.md src/scribe_mcp/server.py src/scribe_mcp/bridges/runtime.py src/scribe_mcp/bridges/registry.py`.
- [x] <!-- id: p6-council-runtime-auth-compat --> `council_mcp` SSE/bootstrap paths can connect to authenticated Scribe transport using the final public auth/env contract while preserving stdio fallback. Proof: 2026-04-05 — added `src/council_mcp/config/scribe_transport.py` so `src/council_mcp/web/mcp_client.py`, `src/council_mcp/server.py`, and `src/council_mcp/cli/connect_cmd.py` all resolve the same authenticated Scribe transport settings and the same core-vs-optional-extension install guidance (`scribe-mcp` for core transport, `scribe-council` only for optional extension assets); `src/council_mcp/config/__init__.py` now carries `council.scribe.remote_auth_token` plus env loading for canonical `SCRIBE_REMOTE_AUTH_TOKEN` with fallback aliases to `SCRIBE_TRANSPORT_AUTH_TOKEN` / `SCRIBE_AUTH_TOKEN`; `src/council_mcp/config/env_schema.py` now registers those public names; and `src/council_mcp/web/mcp_client.py` / `src/council_mcp/server.py` now send `Authorization` + `x-scribe-auth` headers on authenticated SSE while failing clearly on missing or rejected auth instead of silently downgrading. Verification: `python -m compileall -q src/council_mcp/web/mcp_client.py src/council_mcp/server.py src/council_mcp/config/__init__.py src/council_mcp/config/env_schema.py src/council_mcp/config/scribe_transport.py src/council_mcp/cli/connect_cmd.py`; `PYTHONPATH=src python - <<'PY' ...` with explicit assertions for `verified: pool_sse_success`, `verified: pool_missing_token_failure`, `verified: pool_auth_rejection_failure`, `verified: pool_stdio_fallback`, and `verified: server_sse_success`.
- [x] <!-- id: p6-council-boundary-cleanup --> `council_mcp` no longer uses direct `scribe_mcp.storage.*` / `scribe_mcp.utils.*` imports or sibling-repo launch assumptions as public export truth. Proof: 2026-04-05 — `src/council_mcp/services/mcp_servers.py` now emits the installed-package Scribe stdio contract (`scribe-server` + `SCRIBE_ROOT` and default sqlite `.scribe/state/scribe.db`) for both local system metadata and downstream exports instead of sibling-checkout `cd .../scribe_mcp && python -m server`; `src/council_mcp/web/routes/hooks.py` now writes its local hook audit overlay directly without `sys.path` injection or `scribe_mcp.utils.tool_logger`; `src/council_mcp/bridges/scribe_storage_api.py` now routes the deprecated compatibility shim through `ScribeMCPClient` instead of `scribe_mcp.storage.sqlite`; repo-root `.mcp.json` and `opencode.json` now use local overlay `scribe-server` entries rooted at the `council_mcp` workspace; and `docs/MCP_SERVERS.md` now marks repo-root overlays as local operator guidance while naming the installed `scribe-server` contract as the public export truth. Verification: `python -m compileall -q src/council_mcp/services/mcp_servers.py src/council_mcp/web/routes/hooks.py src/council_mcp/bridges/scribe_storage_api.py tests/test_codex_export.py tests/test_mcp_codex_export.py`; `pytest -q tests/test_codex_export.py tests/test_mcp_codex_export.py::TestGatherMcpServers::test_downstream_includes_knowledge_managed_server tests/test_mcp_codex_export.py::test_audit_export_drift_uses_shared_post_policy_surface tests/test_mcp_codex_export.py::TestTomlRoundTrip::test_downstream_scribe_config_uses_public_install_contract` (`10 passed in 0.93s`); `python -m json.tool .mcp.json`; `python -m json.tool opencode.json`; and `rg -n "scribe_mcp\.(storage|utils)|sys\.path|cd .*scribe_mcp && exec python -m server" src/council_mcp/services/mcp_servers.py src/council_mcp/web/routes/hooks.py src/council_mcp/bridges/scribe_storage_api.py .mcp.json opencode.json docs/MCP_SERVERS.md tests/test_codex_export.py tests/test_mcp_codex_export.py` (`no matches`).
- [x] <!-- id: p6-council-deploy-export-lockstep --> `council_mcp` Docker/dev-serve/scaffold/export/docs/tests now carry the final public Scribe contract in lockstep: scaffold defaults keep loopback-local as the default posture, optional remote/client uses canonical `SCRIBE_REMOTE_AUTH_TOKEN`, deploy secrets/manifests thread the matching remote vs transport auth names, dedicated docs distinguish managed private-mesh/Tailscale guidance from unsupported casual public exposure, and the bounded regression lane passed. Proof: 2026-04-05 — updated `src/council_mcp/templates/defaults/council.yaml`, `src/council_mcp/templates/scaffold/.env.example.j2`, `src/council_mcp/cli/init_cmd.py`, `deploy/docker-compose.yaml`, `deploy/docker-compose.dev.yaml`, `deploy/docker-entrypoint.sh`, `deploy/scripts/deploy.sh`, `deploy/scripts/setup-hetzner.sh`, `docs/MCP_SERVERS.md`, `docs/CLI_REFERENCE.md`, `tests/test_multi_repo_scribe.py`, `tests/test_connect_serve.py`, and `tests/test_connect_serve_config.py` so installed-package `scribe-server` remains the public truth, `scribe-council` stays optional, repo-root overlays stay local-only, and `0.0.0.0` guidance is limited to operator-managed private mesh posture. Verification: `python -m compileall -q /home/austin/projects/MCP_SPINE/council_mcp/src/council_mcp/cli/init_cmd.py /home/austin/projects/MCP_SPINE/council_mcp/tests/test_multi_repo_scribe.py /home/austin/projects/MCP_SPINE/council_mcp/tests/bridges/test_scribe_mcp_client.py /home/austin/projects/MCP_SPINE/council_mcp/tests/test_connect_serve.py /home/austin/projects/MCP_SPINE/council_mcp/tests/test_connect_serve_config.py`; `pytest -q tests/test_multi_repo_scribe.py tests/bridges/test_scribe_mcp_client.py tests/test_connect_serve.py tests/test_connect_serve_config.py` (`147 passed in 24.93s`).
- [ ] <!-- id: p6-compatibility-matrix --> One built-artifact compatibility matrix/signoff bundle records the supported `scribe-mcp` + `scribe-council` + `council_mcp` combinations and makes the post-wave `v3` decision explicit.
- [ ] <!-- id: p6-final-file-map-last --> README and the final file-map artifact are published only after structural work is frozen; `docs/RELEASE_FILE_MAP.md` becomes the detailed final map and README links to it instead of acting as an interim tree during churn.

### Explicit defer list (not part of the 2.2 ship history or the next synchronized release wave)
- [ ] <!-- id: d1-shared-host-coordinator --> Port a `knowledge_mcp`-style repo-scoped lease/shared-host coordinator into Scribe.
- [ ] <!-- id: d2-remote-tool-dispatch --> Replace storage-only remote mode with full remote tool execution / thin-client runtime semantics.
- [ ] <!-- id: d3-major-decomposition --> Fully decompose `server.py`, `doc_management/manager.py`, `read_file.py`, `append_entry.py`, `rotate_log.py`, and Postgres storage beyond blocker-oriented extractions.
- [ ] <!-- id: d4-test-taxonomy-reorg --> Perform the full domain-package test reorganization after the bounded Phase 4 release-surface cleanup is complete.
