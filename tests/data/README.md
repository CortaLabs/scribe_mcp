# Test data

Keep durable checked-in test inputs here.

Generated artifacts, demos, and benchmark output do **not** belong in `tests/data/`
or anywhere else under `tests/`; they should live in their dedicated non-test paths.
